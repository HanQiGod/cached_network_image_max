name: cached_network_image_max
description: Flutter 升级 3.35.7 之后，对 cached_network_image 插件的升级（After Flutter upgrade 3.35.7, the upgrade of cached_network_image plugins.）。
homepage: https://github.com/HanQiGod/cached_network_image_max
topics:
  - cache
  - image
  - network-image
version: 3.5.0

environment:
  sdk: ^3.0.0
  flutter: '>=3.10.0'

dependencies:
  cached_network_image_max_platform_interface: ^4.2.0
  cached_network_image_max_web: ^1.4.0
  flutter:
    sdk: flutter
  flutter_cache_manager: ^3.4.1
  octo_image: ^2.1.0

dev_dependencies:
  file: '>=7.0.0 <8.0.0'
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter
  mocktail: ^1.0.4
